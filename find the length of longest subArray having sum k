def max(arr,n,k):
  sum=0
  maxlen=0
  
  dict1=dict()
  for i in range(n):
    sum+=arr[i]
    if(sum==k):
      maxlen=i+1
     elif(sum-k):
      maxlen=max(maxlen,i-mydict[sum-k])
     if sum not in dict1:
      mydict[sum]=i
return maxlen
  
